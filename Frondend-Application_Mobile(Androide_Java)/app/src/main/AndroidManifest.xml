<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aninterface.interactive.scolaire_activity">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity"></activity>
        <activity android:name=".AfficheListeActivity" />
        <activity android:name=".MoyenneDesNotesActivity" />
        <activity android:name=".NomDesParticipantActivity" />
        <activity android:name=".NombreParticipantActivity" />
        <activity android:name=".NoteActivity" />
        <activity android:name=".AjoutActivity" />
        <activity android:name=".InscriptionActivity" />
        <activity android:name=".MiseAjourActivity" />
        <activity android:name=".InscriptionALActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>